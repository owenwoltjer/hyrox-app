/**
 * /today — Today's training session view.
 *
 * UI spec pending. This stub exports a valid Next.js page component
 * so the route compiles and the tab bar can link to it.
 *
 * TODO: Replace placeholder with full design implementation.
 */
export default function TodayPage() {
  return (
    <main className="flex-1 p-4">
      <p className="text-brand-subtle text-sm">Today — UI coming soon</p>
    </main>
  );
}
